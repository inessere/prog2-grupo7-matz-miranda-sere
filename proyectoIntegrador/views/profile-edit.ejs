<!-- Include Head -->



<body>
	<% if (locals.user != undefined) { %>
		<%- include('./partials/headLogueado') %>
	 <% } else { %>
		<%- include('./partials/head') %>
	<% } %>
	<!-- Novedades -->
	<div class="container products-wrapper">
		<div class="row">
			<div class="col-12">
				<h2 class="products-title">Editar mi perfil</h2>
			</div>
			<!-- Show the "ultimos" products -->
			<% if(locals.user) {%>
			<form action="/users/profile-edit" method="post">
				<input type="hidden" name="id" value="<%= locals.user.id%>">
				
				<div>
					<label for="email">Email: </label>
					<input id="email" type="email" name="email" value="<%= locals.user.mail %>">
					<% if (locals.errors && errors.email) { %>
						 <%= errors.email.msg %>
					<% } %>
				</div>

				<div>
					<label for="contraseña">Contraseña: </label>
					<input id="contraseña" type="password" name="contraseña" value="<%= locals.user.contrasenia %>">
					<% if (locals.errors && errors.contrasenia) { %>
						<%= errors.contrasenia.msg %>
				   <% } %>
				</div>

				<div>
					<label for="cumpleaños">Fecha de nacimiento: </label>
					<input id="cumpleaños" type="text" name="cumpleaños" value="<%= locals.user.fecha %>">
				</div>

				<div>
					<label for="dni">Numero de documento: </label>
					<input id="dni" type="text" name="dni" value="<%= locals.user.numeroDocumento %>">
				</div>

				<div>
					<label for="foto">Foto de perfil: </label>
					<input id="foto" type="file" name="foto" value="<%= locals.user.fotodeperfil %>">
				</div>
				<div>
					<label for="usuario">Usuario: </label>
							<input id="usuario" type="text" name="username" value="<%= locals.user.username %>"/>

					</div>
				<div>
					<button type="submit">Enviar</button>
				</div>
			</form>
			<% } else { %>
			 <p>Debes iniciar sesion para poder editar tu perfil</p>
		<% }  %>
 


	<!-- footer -->
	<footer class="main-footer">
		<div class="container">
			<div class="row">
				<div class="col-12 col-lg-4">
					<article class="footer-data">
						<i class="fas fa-credit-card"></i>
						<h2>Pagá con tarjeta o en efectivo</h2>
						<p>Con Mercado Liebre Cash, tenés cuotas sin interés con tarjeta o efectivo en puntos de pago.
							¡Y
							siempre es seguro!</p>
					</article>
				</div>
				<div class="col-12 col-lg-4">
					<article class="footer-data">
						<i class="fas fa-gift"></i>
						<h2>Envío gratis desde $ 1.999</h2>
						<p>Solo por estar registrado en Mercado Liebre tenés envíos gratis en miles de productos. Es un
							beneficio de Mercado Puntos.</p>
					</article>
				</div>
				<div class="col-12 col-lg-4">
					<article class="footer-data">
						<i class="fas fa-user-shield"></i>
						<h2>Seguridad, de principio a fin</h2>
						<p>¿No te gusta? ¡Devolvelo! En Mercado Liebre, no hay nada que no puedas hacer, porque estás
							siempre protegido.</p>
					</article>
				</div>
			</div>
		</div>
	</footer>
	<!-- /footer -->
	<%- include('./partials/footer') %>
</body>

</html>